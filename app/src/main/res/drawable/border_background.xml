<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Background color of the container -->
    <stroke
        android:width="2dp"
        android:color="@android:color/darker_gray"/> <!-- Border color and width -->
    <corners android:radius="8dp"/> <!-- Rounded corners (optional) -->
</shape>
